# RegFlow runtime dependencies.
# Install:  pip install -r requirements.txt
# (Or, equivalently, from the project root:  pip install -e .)

# --- Orchestration ---------------------------------------------------------
langgraph>=0.2.50
langchain-core>=0.3.20
langchain-community>=0.3.10
prefect>=3.0.0

# --- LLM provider abstraction (Ollama local + vLLM/OpenAI-compatible remote)
litellm>=1.55.0
ollama>=0.4.0

# --- Embeddings ------------------------------------------------------------
sentence-transformers>=3.3.0
torch>=2.4.0

# --- Vector store ----------------------------------------------------------
weaviate-client>=4.9.0

# --- Knowledge graph -------------------------------------------------------
neo4j>=5.25.0

# --- Relational + object stores --------------------------------------------
psycopg[binary]>=3.2.0
SQLAlchemy>=2.0.36
alembic>=1.14.0
minio>=7.2.0

# --- API layer -------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# --- UI layer --------------------------------------------------------------
gradio>=4.0.0

# --- Ingestion (HTTP, parsers) ---------------------------------------------
httpx>=0.28.0
lxml>=5.3.0
beautifulsoup4>=4.12.0
pypdf>=5.1.0
feedparser>=6.0.11

# --- Observability ---------------------------------------------------------
structlog>=24.4.0
prometheus-client>=0.21.0
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
langfuse>=2.50.0

# --- Utilities -------------------------------------------------------------
PyYAML>=6.0.2
tenacity>=9.0.0
python-dateutil>=2.9.0
deepdiff>=8.0.1
